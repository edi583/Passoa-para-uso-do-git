-- Criar nova pasta e salvar as alterações.

-- Git Terminal:
   git init  
   git status
   git add .
   git commit -m '"Nome do commit"

-- Git hub (On line):
   Novo repositório 
   Dar nome ao repositório
   Descrição opcional
   Adcionar RIDMI
   Clicar em repositório
   Copiar o link de endereço da pagina

-- Git terminal:
   git remote add oringin (colar o endereço do link)
   git add .
   git push (Obs: caso pode ser pedido o endereço do link novamente)
   git add oringin ( colar o endereço do link)
   git push (Obs: ira pedir em qual branch será incluido)
             
	Copiar o endereço que aparece na mensagem anterior.
	Ex: git push --set - upstream origin master

-- Git hub (On line):
   Atualizar a pagina 
   Ira aparecer em qual branch te a alteração
   clicar em branch.
	
    